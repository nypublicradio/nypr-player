machine:
  node:
    version: 8
  environment:
    JOBS: 2

dependencies:
  override:
    - yarn --pure-lockfile

test:
  override:
    - yarn test

deployment:
  docs:
    branch: master
    commands:
      - git checkout gh-pages; git checkout master
      - ./node_modules/ember-cli/bin/ember deploy production
