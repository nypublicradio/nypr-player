import Component from '@ember/component';
import layout from '../../templates/components/nypr-player/backdrop';

export default Component.extend({
  layout,
  classNames: ['nypr-player-backdrop']
});
