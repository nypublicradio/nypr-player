import { moduleForComponent, test } from 'ember-qunit';
// import hbs from 'htmlbars-inline-precompile';

moduleForComponent('nypr-player', 'Integration | Component | nypr player', {
  integration: true
});

test('tbd', function(assert) {
  assert.ok(true);
});
