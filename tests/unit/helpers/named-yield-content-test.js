
import { namedYieldContent } from 'dummy/helpers/named-yield-content';
import { module, test } from 'qunit';

module('Unit | Helper | named yield content');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = namedYieldContent([42]);
  assert.ok(result);
});

